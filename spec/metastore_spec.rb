require 'spec_helper'

describe Metastore do
  it 'has a version number' do
    expect(Metastore::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
